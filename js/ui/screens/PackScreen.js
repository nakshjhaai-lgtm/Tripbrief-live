import { state } from '../../state.js';
import { escapeHtml } from '../../utils/text.js';
export function PackScreen() { const p = state.pack; return `<section class="screen-enter reveal-stagger"><p class="eyebrow">Offline Lifeboat</p><h1 class="display" style="font-size:clamp(3rem,10vw,6rem);margin:.2rem 0 1rem">Pack</h1>${p ? `<section class="grid two"><article class="card pad pack-ready"><h2>${escapeHtml(p.summary.status)}</h2><p>${escapeHtml(p.summary.tripTitle)} · ${escapeHtml(p.summary.dateRange)}</p><p>${p.summary.readinessScore}% ready</p></article><article class="card pad"><h2>Exports</h2><div class="action-row"><button class="button" id="copyPack">Copy text</button><button class="button secondary" id="downloadMd">Markdown</button><button class="button secondary" id="downloadHtml">Printable HTML</button><button class="button secondary" id="downloadIcs">ICS</button></div></article></section><h2>Family-safe plain text</h2><pre class="export-box">${escapeHtml(p.exports.plainText)}</pre>` : '<div class="empty">No pack yet. Add travel input first.</div>'}</section>`; }
