import { state } from '../../state.js';
import { rawCard } from '../components.js';
export function DumpScreen() { return `<section class="dump-zone screen-enter reveal-stagger">
  <div><p class="eyebrow">Universal Dump</p><h1 class="display" style="font-size:clamp(2.8rem,9vw,6.4rem);margin:.2rem 0">Drop it here.<br>Relax after.</h1><p class="lede narrow">TripBrief will save the original proof instantly, then read what it can. Anything uncertain waits for your review.</p></div>
  <article class="drop-card card"><div><p class="eyebrow">Calm capture</p><h2>Dump travel stuff here.</h2><p>Screenshots, PDFs, copied confirmations, hotel notes, boarding passes, or plain text.</p><ol class="mini-steps"><li>Save offline</li><li>Read quietly</li><li>Show only what needs checking</li></ol><div class="action-row"><button class="button brass" id="clipboardBtn">Import clipboard</button><label class="button secondary" for="fileInput">Add files</label><label class="button secondary" for="cameraInput">Take photo</label></div><input id="fileInput" type="file" accept="image/*,.pdf,text/plain" multiple hidden><input id="cameraInput" type="file" accept="image/*" capture="environment" hidden></div></article>
  <article class="card pad"><div class="field"><label for="manualText">Paste text</label><textarea id="manualText" rows="7" placeholder="Paste booking email text, SMS, WhatsApp itinerary, hotel notes…"></textarea></div><button class="button" id="saveTextBtn" style="margin-top:.75rem">Save gently</button></article>
  <section><h2>Saved proof inbox</h2><div class="item-list">${state.rawItems.length ? state.rawItems.map(rawCard).join('') : '<div class="empty">Nothing saved yet. Add anything messy — a screenshot, message, or note is enough.</div>'}</div></section>
</section>`; }
